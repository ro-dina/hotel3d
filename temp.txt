すいません、Map.tsxのTopologyやObjects, containerRef, ComposableMap, ZoomableGroup, Geographies, Geography, などの名前が見つからないというエラーが出ています。これらを修正してください。しかし、そもそもテスト実行しているのですが今特にエラーがでていないので、使っていないような内容であれば削除してください。

layer.setStyleのエラーは解消してください。また134, 141行目のanyのエラーも解消してください。

今現在、ズーム倍率によって地方単位、都道府県単位、市区町村単位で分けて表示されるようになっています。この中でも、市区町村単位で表示されている時、その都道府県の中身だけが分割してパネル表示されるようになっています。これは不便なので、その周囲の県も読み込むようにしてください。現在自分が見ている県の外側、端っこを見ているような状況だと、県境を少し超えたようなところも同時に見てみたいという感想が出ます。

次に、都道府県単位の表示のときに、都道府県をクリックしてズームインする倍率を下げてください。

自分のカーソルの先には、その地方、都道府県、市区町村が何であるかを表示するようにしてください。\

今画面に映されている範囲のすべての市区町村を読み込んでほしいです。しかし、それ以外を読み込むと負荷がすごいので、写っていない部分は読み込まなくてよいです。しかし、写されている画面が移動したときには、スムーズに必要な部分だけを読み込むというような処理にしてください。

